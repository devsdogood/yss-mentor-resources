const ResourceCollection: React.FC = ({ children }) => {
    return (
        <div style={{border: 'orange 1px solid'}}>
            {children}
        </div>
    );
}

export default ResourceCollection;
