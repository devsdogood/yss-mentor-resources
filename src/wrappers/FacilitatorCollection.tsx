const FacilitatorCollection: React.FC = ({ children }) => {
    return (
        <div style={{border: 'purple 1px solid'}}>
            {children}
        </div>
    );
}

export default FacilitatorCollection;
