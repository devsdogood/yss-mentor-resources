const DocumentCollection: React.FC = ({ children }) => {
    return (
        <div className="columns is-multiline">
            {children}
        </div>
    );
};

export default DocumentCollection;
